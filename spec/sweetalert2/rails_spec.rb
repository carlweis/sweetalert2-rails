require "spec_helper"

RSpec.describe Sweetalert2::Rails do
  it "has a version number" do
    expect(Sweetalert2::Rails::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
